# dsh-view-manager bundle patch: contributes one profile row.
# The node half (lib/index.js) is a no-op host; the browser half ships via
# exports["./client"] and the package.json `dsh.client` declaration.

- insert:
    - id: dsh-view-manager
      name: 'dsh-view-manager'
